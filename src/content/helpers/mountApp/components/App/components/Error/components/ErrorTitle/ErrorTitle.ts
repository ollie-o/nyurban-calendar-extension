import styled from 'styled-components';

/** Styled title for error display. */
export const ErrorTitle = styled.h3`
  margin: 0 0 10px 0;
  font-size: 16px;
  font-weight: bold;
  color: #8b0000;
`;
